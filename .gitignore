# dependencies
**/node_modules/
/.pnp
.pnp.*

# next.js / build
**/.next/
**/out/
/build

# 생성된 4컷 이미지 (용량 큼, 스크립트로 재생성 가능)
# → 배포(Vercel)에서 이미지를 보여주려고 web/public/images 는 올림.
#   pipeline 원본(output/images)만 제외해 용량 절약.
**/output/images/

# pipeline 원본 데이터는 제외 (web/public 으로 복사된 것만 배포에 포함)
pipeline/output/
pipeline/data/

# env / secrets (API 키 절대 커밋 금지)
.env
.env.*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# misc / os / editor
.DS_Store
*.pem
*.log
npm-debug.log*
yarn-debug.log*
.vercel
.vscode/
.idea/